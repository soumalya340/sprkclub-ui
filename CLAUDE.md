# Dev server

Never start the dev server (e.g. `npm run dev`, `next dev`) yourself, in the foreground, in the background, or via any script. The user always runs the dev server themselves in a separate terminal. If you need it running to verify a change, ask the user to start it (or confirm it's already running) instead of launching it yourself.

@AGENTS.md
